fields:
    name:
        label: azeyn.galleries::lang.models.name
        span: left
        required: true
    slug:
        label: azeyn.galleries::lang.models.slug
        preset:
            field: name
            type: slug
        span: right
        required: true
